<%= content_for(:header_text, @event.title) %>

<div class="col-md-9">
  <h2>RSVP</h2>

  <h3>You're almost signed up! We just have a few questions...</h3>

  <%= render 'rsvps/form' %>

  <%= render 'shared/actions', links: [
    ['Back', events_path]
  ] %>
</div>
