Howdy, OpenHatch Events administrator!

A new event, "<%= @event.title %>", has been posted to OpenHatch Events!

<%= render 'new_event_detail' %>

<% if @event.published %>
This event is already published, so you should be able to find it here: <%= events_url %>
<% else %>
Go see the list of unpublished events to see if this is worth approving by clicking this link: <%= unpublished_events_url %>
<% end %>
